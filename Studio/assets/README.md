# Assets

This directory stores non-primary project assets and archived code.

- `legacy-server/`: previous FastAPI, VLM, SD, and UDP listener implementation. It is kept only for reference and is not used by the current frontend + Vite UDP bridge flow.
